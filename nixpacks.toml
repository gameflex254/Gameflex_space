providers = ["node"]

[variables]
NODE_ENV = "production"
NITRO_PRESET = "node_server"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
